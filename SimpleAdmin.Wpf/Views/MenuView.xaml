<UserControl
    x:Class="SimpleAdmin.Wpf.Views.MenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:markup="clr-namespace:SimpleAdmin.Wpf.Windows.Markup"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SimpleAdmin.Wpf.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:MenuViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <markup:Di ServiceType="{x:Type vm:MenuViewModel}" />
    </UserControl.DataContext>

    <ListBox SelectionChanged="ListBox_SelectionChanged"
        DisplayMemberPath="Header"
        ItemsSource="{Binding MenuItems}"
        SelectedItem="{Binding SelectedMenuItem}" />
</UserControl>

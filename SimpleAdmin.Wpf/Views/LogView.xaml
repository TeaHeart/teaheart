<UserControl
    x:Class="SimpleAdmin.Wpf.Views.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:markup="clr-namespace:SimpleAdmin.Wpf.Windows.Markup"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:SimpleAdmin.Wpf.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:LogViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <markup:Di ServiceType="{x:Type vm:LogViewModel}" />
    </UserControl.DataContext>

    <TextBox
        HorizontalScrollBarVisibility="Auto"
        IsReadOnly="True"
        ScrollViewer.ScrollChanged="TextBox_ScrollChanged"
        Text="{Binding LogText, Mode=OneWay}"
        TextChanged="TextBox_TextChanged"
        TextWrapping="Wrap"
        VerticalScrollBarVisibility="Auto">
        <TextBox.ContextMenu>
            <ContextMenu>
                <MenuItem Command="{Binding ClearCommand}" Header="清空" />
            </ContextMenu>
        </TextBox.ContextMenu>
    </TextBox>
</UserControl>
